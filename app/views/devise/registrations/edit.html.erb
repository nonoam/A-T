<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
  
  <div class="md:flex md:items-center md:justify-between mb-8">
    <div class="flex-1 min-w-0">
      <h2 class="text-2xl font-bold leading-7 text-gray-900 sm:text-3xl sm:truncate">
        Configuración de la Cuenta
      </h2>
      <p class="mt-1 text-sm text-gray-500">
        Gestiona tu información personal y la seguridad de tu acceso.
      </p>
    </div>
    <div class="mt-4 flex md:mt-0 md:ml-4">
      <%= link_to "Volver al inicio", root_path, class: "inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
    </div>
  </div>

  <div class="space-y-6">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class: "space-y-6" }) do |f| %>
      
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="bg-white shadow overflow-hidden sm:rounded-lg">
        <div class="px-4 py-5 sm:px-6 bg-gray-50 border-b border-gray-200">
          <h3 class="text-lg leading-6 font-medium text-gray-900">Información Personal</h3>
          <p class="mt-1 max-w-2xl text-sm text-gray-500">Estos datos se mostrarán en tu perfil público y reservas.</p>
        </div>
        
        <div class="px-4 py-5 sm:p-6 space-y-6">
          <div class="grid grid-cols-1 gap-y-6 gap-x-4 sm:grid-cols-6">
            <div class="sm:col-span-6 flex items-center">
              <span class="h-16 w-16 rounded-full overflow-hidden bg-gray-100 flex items-center justify-center">
                <span class="text-xl font-bold text-gray-500">
                  <%= resource.email[0].upcase %>
                </span>
              </span>
              <div class="ml-4">
                <button type="button" class="bg-white py-2 px-3 border border-gray-300 rounded-md shadow-sm text-sm leading-4 font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                  Cambiar foto (Próximamente)
                </button>
              </div>
            </div>

            <div class="sm:col-span-3">
              <%= f.label :first_name, "Nombre", class: "block text-sm font-medium text-gray-700" %>
              <div class="mt-1">
                <%= f.text_field :first_name, class: "shadow-sm focus:ring-indigo-500 focus:border-indigo-500 block w-full sm:text-sm border-gray-300 rounded-md py-2 px-3" %>
              </div>
            </div>

            <div class="sm:col-span-3">
              <%= f.label :last_name, "Apellido", class: "block text-sm font-medium text-gray-700" %>
              <div class="mt-1">
                <%= f.text_field :last_name, class: "shadow-sm focus:ring-indigo-500 focus:border-indigo-500 block w-full sm:text-sm border-gray-300 rounded-md py-2 px-3" %>
              </div>
            </div>

            <div class="sm:col-span-4">
              <%= f.label :email, "Correo Electrónico", class: "block text-sm font-medium text-gray-700" %>
              <div class="mt-1">
                <%= f.email_field :email, autocomplete: "email", class: "shadow-sm focus:ring-indigo-500 focus:border-indigo-500 block w-full sm:text-sm border-gray-300 rounded-md py-2 px-3" %>
              </div>
              <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
                <div class="mt-2 text-sm text-yellow-600 bg-yellow-50 p-2 rounded">
                  Esperando confirmación para: <%= resource.unconfirmed_email %>
                </div>
              <% end %>
            </div>
            
            <div class="sm:col-span-2">
              <label class="block text-sm font-medium text-gray-700">Tipo de cuenta</label>
              <div class="mt-1">
                <input type="text" disabled value="<%= resource.role&.humanize %>" class="bg-gray-100 block w-full sm:text-sm border-gray-300 rounded-md py-2 px-3 text-gray-500 cursor-not-allowed">
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="bg-white shadow overflow-hidden sm:rounded-lg">
        <div class="px-4 py-5 sm:px-6 bg-gray-50 border-b border-gray-200">
          <h3 class="text-lg leading-6 font-medium text-gray-900">Seguridad</h3>
          <p class="mt-1 max-w-2xl text-sm text-gray-500">Actualiza tu contraseña. Déjalo en blanco si no quieres cambiarla.</p>
        </div>
        
        <div class="px-4 py-5 sm:p-6 space-y-6">
          <div class="grid grid-cols-1 gap-y-6 gap-x-4 sm:grid-cols-6">
            
            <div class="sm:col-span-3">
              <%= f.label :password, "Nueva Contraseña", class: "block text-sm font-medium text-gray-700" %>
              <div class="mt-1">
                <%= f.password_field :password, autocomplete: "new-password", class: "shadow-sm focus:ring-indigo-500 focus:border-indigo-500 block w-full sm:text-sm border-gray-300 rounded-md py-2 px-3" %>
              </div>
              <p class="mt-1 text-xs text-gray-500">Mínimo <%= @minimum_password_length %> caracteres si la cambias.</p>
            </div>

            <div class="sm:col-span-3">
              <%= f.label :password_confirmation, "Confirmar Nueva Contraseña", class: "block text-sm font-medium text-gray-700" %>
              <div class="mt-1">
                <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "shadow-sm focus:ring-indigo-500 focus:border-indigo-500 block w-full sm:text-sm border-gray-300 rounded-md py-2 px-3" %>
              </div>
            </div>

            <div class="sm:col-span-6 border-t border-gray-100 pt-6">
              <h4 class="text-sm font-medium text-gray-900 mb-2">Confirmar cambios</h4>
              <p class="text-sm text-gray-500 mb-4">Necesitamos tu contraseña actual para guardar cualquier cambio en esta página.</p>
              
              <div class="max-w-md">
                <%= f.label :current_password, "Contraseña Actual", class: "block text-sm font-medium text-gray-700" %>
                <div class="mt-1 relative rounded-md shadow-sm">
                  <%= f.password_field :current_password, autocomplete: "current-password", class: "focus:ring-indigo-500 focus:border-indigo-500 block w-full sm:text-sm border-gray-300 rounded-md py-2 px-3" %>
                </div>
              </div>
            </div>
          </div>
        </div>
        
        <div class="px-4 py-3 bg-gray-50 text-right sm:px-6">
          <%= f.submit "Guardar Cambios", class: "inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 cursor-pointer" %>
        </div>
      </div>
    <% end %>

    <div class="bg-red-50 shadow sm:rounded-lg border border-red-100 mt-10">
      <div class="px-4 py-5 sm:p-6">
        <h3 class="text-lg leading-6 font-medium text-red-800">Zona de Peligro</h3>
        <div class="mt-2 max-w-xl text-sm text-red-700">
          <p>Una vez que elimines tu cuenta, no hay vuelta atrás. Por favor, asegúrate de querer hacer esto.</p>
        </div>
        <div class="mt-5">
          <%= button_to "Eliminar mi cuenta", registration_path(resource_name), data: { confirm: "¿Estás completamente seguro? Esta acción es irreversible.", turbo_confirm: "¿Estás completamente seguro? Esta acción es irreversible." }, method: :delete, class: "inline-flex items-center justify-center px-4 py-2 border border-transparent font-medium rounded-md text-red-700 bg-red-100 hover:bg-red-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 sm:text-sm cursor-pointer" %>
        </div>
      </div>
    </div>

  </div>
</div>