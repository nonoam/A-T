<div class="min-h-screen bg-gray-50 py-12 px-4 sm:px-6 lg:px-8 flex items-center justify-center">
  <div class="max-w-4xl w-full">
    
    <div class="mb-6">
      <%= link_to service_path(@service), class: "text-sm text-gray-500 hover:text-gray-900 flex items-center transition" do %>
        <svg class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
        </svg>
        Volver a la clase
      <% end %>
    </div>

    <div class="bg-white rounded-2xl shadow-xl overflow-hidden grid grid-cols-1 md:grid-cols-2">
      
      <div class="bg-indigo-900 p-8 text-white relative overflow-hidden flex flex-col justify-between">
        <div class="absolute top-0 right-0 -mr-16 -mt-16 w-64 h-64 bg-indigo-500 rounded-full opacity-20 blur-3xl"></div>
        <div class="absolute bottom-0 left-0 -ml-10 -mb-10 w-40 h-40 bg-purple-500 rounded-full opacity-20 blur-2xl"></div>
        
        <div class="relative z-10">
          <h2 class="text-xs font-bold tracking-widest uppercase text-indigo-300 mb-2">Resumen de reserva</h2>
          <h1 class="text-3xl font-bold mb-4 leading-tight"><%= @service.title %></h1>
          
          <div class="flex items-center space-x-3 text-indigo-100 text-sm mb-6">
            <div class="flex items-center bg-indigo-800/50 px-3 py-1 rounded-full">
              <svg class="h-4 w-4 mr-1.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              <%= @service.duration %> min
            </div>
            <div class="flex items-center bg-indigo-800/50 px-3 py-1 rounded-full">
               <svg class="h-4 w-4 mr-1.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
              </svg>
              <%= @service.modality.humanize %>
            </div>
          </div>

          <div class="border-t border-indigo-700 pt-6 mt-6">
            <div class="flex items-center">
              <div class="h-10 w-10 rounded-full bg-indigo-400 flex items-center justify-center text-white font-bold text-lg">
                <%= @service.user.first_name[0] %>
              </div>
              <div class="ml-3">
                <p class="text-sm font-medium text-white">Prof. <%= @service.user.full_name %></p>
                <p class="text-xs text-indigo-300">Instructor Verificado</p>
              </div>
            </div>
          </div>
        </div>

        <div class="relative z-10 mt-10">
          <p class="text-sm text-indigo-300 mb-1">Total a pagar</p>
          <div class="flex items-baseline">
            <span class="text-4xl font-extrabold text-white">$<%= @service.price_cents %></span>
            <span class="ml-2 text-indigo-300">/ sesión</span>
          </div>
        </div>
      </div>

      <div class="p-8 md:p-10 flex flex-col justify-center bg-white">
        <h3 class="text-2xl font-bold text-gray-900 mb-6">Finaliza tu reserva</h3>
        
        <%= form_with(model: [@service, @booking], local: true, class: "space-y-6") do |f| %>
          
          <% if @booking.errors.any? %>
            <div class="bg-red-50 p-4 rounded-lg border border-red-100">
              <h4 class="text-sm font-medium text-red-800 mb-2">Ups, algo salió mal:</h4>
              <ul class="list-disc list-inside text-sm text-red-700">
                <% @booking.errors.full_messages.each do |msg| %>
                  <li><%= msg %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div>
            <%= f.label :start_time, "Elige fecha y hora", class: "block text-sm font-medium text-gray-700 mb-2" %>
            <%= f.datetime_field :start_time, class: "w-full rounded-lg border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 py-3 px-4 text-gray-700 bg-gray-50", required: true %>
            <p class="mt-2 text-xs text-gray-500">La sesión dura <%= @service.duration %> minutos.</p>
          </div>

          <div class="pt-4">
            <%= f.submit "Confirmar Reserva", class: "w-full flex justify-center py-4 px-4 border border-transparent rounded-xl shadow-lg text-base font-bold text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition transform hover:-translate-y-0.5 cursor-pointer" %>
            <p class="mt-4 text-xs text-center text-gray-400">
              Al confirmar, aceptas los términos de servicio de A&N.
            </p>
          </div>
        <% end %>
      </div>

    </div>
  </div>
</div>